Just a plain note. No frontmatter, no headings, no tasks.

Used as a negative-control case so harness assertions can verify the
metadata cache produces empty `headings` / `listItems` arrays and
omits `frontmatter` entirely.
